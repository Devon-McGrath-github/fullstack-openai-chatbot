{
  "name": "root",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "concurrently -k \"pnpm --filter ./server run dev\" \"pnpm --filter ./client run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}